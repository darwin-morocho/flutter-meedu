/// all posible transition animation for the
/// router module
// ignore_for_file: public_member_api_docs

enum Transition {
  fadeIn, // ready
  rightToLeft, // ready
  upToDown, //ready
  downToUp, // ready
  zoom, // ready
  none, // none
  cupertino, // ready
  material, // ready
}
